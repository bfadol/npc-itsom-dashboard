# NPC ITSOM Dashboard — Environment Variables
# Copy this file to .env and fill in the values

# ── Server ──────────────────────────────────────────────────
NODE_ENV=production
PORT=8080

# ── Session (REQUIRED in production) ────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# ── Database ────────────────────────────────────────────────
# Path to SQLite database (default: ./data/admin.db)
DB_PATH=./data/admin.db

# ── Azure App Service ──────────────────────────────────────
# Azure sets these automatically when deployed:
#   WEBSITES_PORT=8080        (Azure overrides the container port)
#   WEBSITE_HOSTNAME=...      (Your app's hostname)
#
# Required App Settings in Azure Portal → Configuration:
#   SESSION_SECRET    → random 32+ char string (Application setting)
#
# Optional:
#   PORT              → defaults to 8080
#   DB_PATH           → defaults to ./data/admin.db
#
# For persistent storage, mount Azure Files to /app/data:
#   Azure Portal → App Service → Configuration → Path Mappings
#   Mount path: /app/data  |  Azure Files share  |  Read/Write
